spring.application.name=catalog
server.port=8090

# Use host.docker.internal to reach the Eureka server running on your host machine
eureka.client.service-url.defaultZone=http://host.docker.internal:8761/eureka

# Use host.docker.internal to reach Postgres running on your host machine
spring.datasource.url=jdbc:postgresql://localhost:5432/Review


spring.datasource.username=postgres
spring.datasource.password=ayoub
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
management.endpoints.web.base-path=/actuator
